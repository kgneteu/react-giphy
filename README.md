# react-giphy
A simple React app that demonstrates the use of the Giphy API. Based on the wellpaidgeek course.

To run the project:

1. Clone this repo
2. Run `yarn`
3. Run `yarn start`

# Technologies
- React (Webpack)
- Redux
- Redux Saga
- Giphy API
